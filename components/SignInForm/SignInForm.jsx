import { FormContainer } from "./SignInForm.styles.js";

const SignInForm = ({ data }) => {
  console.log("=>", data);
  return <FormContainer>Sign in form</FormContainer>;
};

export default SignInForm;
